{
  "dependencies": {
    "@angular/compiler": "^2.2.3",
    "@angular/compiler-cli": "^2.2.3",
    "@angular/core": "^2.2.3",
    "rimraf": "^2.5.4",
    "rxjs": "^5.0.0-rc.4",
    "typescript": "^2.0.10"
  },
  "description": "Base Angular Modules and Directives",
  "name": "lib-ng",
  "scripts": {
    "clean":"rimraf clean",
    "build.success": "rimraf build && ngc -p tsconfig.success.json",
    "build.fail": "rimraf build && ngc -p tsconfig.fail.json"
  },
  "version": "0.0.0"
}
